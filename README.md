# buto-ha-salaryman-u
buto-ha-salaryman with unity

Connected.
NEW PC installed.


```
wsl

cd
cd dev
cd /path/to/directory/

// update something

git add .
git commit -m "some messages"
git push
```

